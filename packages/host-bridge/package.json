{
  "name": "@gitstudio/host-bridge",
  "version": "0.0.0",
  "private": true,
  "license": "Apache-2.0",
  "description": "Host-agnostic message protocol + HostBridge / HostGitAdapter interfaces shared by the VS Code extension and the desktop shell.",
  "exports": {
    "./*": "./src/*.ts"
  },
  "scripts": {
    "check-types": "tsc --noEmit -p tsconfig.json"
  }
}
